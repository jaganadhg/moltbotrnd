# OpenClaw Podman Environment — EXAMPLE
# Copy to .env and customize. NEVER commit .env to version control.

# Gateway authentication token (auto-generated by setup.sh, or set your own)
# Generate: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=CHANGE_ME_TO_A_RANDOM_64_CHAR_HEX_STRING
